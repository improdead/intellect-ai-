/**
 * Custom Next.js configuration to override the default settings
 * @type {import('next').NextConfig}
 */
const userConfig = {
  // Override the default config to remove experimental features
  experimental: null,
}

export default userConfig
